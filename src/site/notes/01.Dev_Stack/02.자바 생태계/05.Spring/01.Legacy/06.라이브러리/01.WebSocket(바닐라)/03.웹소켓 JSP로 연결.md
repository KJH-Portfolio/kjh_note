---
dg-publish: true
작성일: 2025-12-12
tags:
수정일: 2025-12-29T16:46
---
#### Html 부분
```html
<body>
	<%@ include file="/WEB-INF/views/common/menubar.jsp" %>
	<div class="content">
		<div class="innerOuter" style="padding: 5% 10%;">
		
		<h1>개인 채팅 서버</h1>
		<button onclick="connect();">접속</button>
		<button onclick="disconnect();">종료</button>
		<hr>
		<div style="display:flex">
			<div>
				<input type="text" id="chat">
				<button onclick="send();">전송</button>
			</div>
			<div>
				<ul id="userList">
					
				</ul>
			</div>
		</div>
			<div id="chatArea">
			</div>
			<div id="other">
				귓속말 선택된 대상 :
			</div>
		</div>
	</div>
</body>
```
---
#### javaScript 부분(순수js)
```js
let socket;
let otherId = null;

// DOM 요소 참조
const userListEl = document.getElementById("userList");
const otherEl = document.getElementById("other");
const chatAreaEl = document.getElementById("chatArea");
const chatInputEl = document.getElementById("chat");

//클릭시 귓속말 대상 ID가져옴
userListEl.addEventListener("click", function(e) {
	// 내부 li를 클릭시에만 작동
	const targetLi = e.target.closest("li");
	if (targetLi) {
		const id = targetLi.textContent;
		otherEl.textContent = "귓속말 선택된 대상: " + id;
		otherId = id;
	}
});

//귓속말 상태 해제
otherEl.addEventListener("click", function() {
	this.textContent = "";
	otherId = null;
});

//WebSocket로직
function connect() {
	//url 설정
	let url = "ws://localhost:8080/spring/chat";
	if (!socket) {
		socket = new WebSocket(url);
	}
	
	//연결 성공
	socket.onopen = function() {
	};
	
	//데이터 수신 - userList작성 또는 채팅메시지 수신
	socket.onmessage = function(message) {
		// 전달 데이터 JSON 파싱
		let msgJson = JSON.parse(message.data);
		
		//userList를 수신했다면
		료
function disconnect() {
	if(socket) {
		socket.close();
	}
}
```