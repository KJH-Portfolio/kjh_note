---
{"dg-publish":true,"permalink":"/01-dev-stack/02/05-spring/01-legacy/03/02/","dg-note-properties":{"작성일":"2026-01-07T22:40","수정일":"2025-12-16T14:15"}}
---


# 주요 기능 심화 (암호화, 페이징)

> [!summary] 요약
>
>   * **암호화**: `BCryptPasswordEncoder`를 사용하여 복호화가 불가능한 단방향 암호화를 수행.
>   * **페이징**: `PageInfo` 객체로 정보를 관리하고, MyBatis `RowBounds`로 DB 부하를 줄여 조회.

## 1\. 비밀번호 암호화 (BCrypt)

보안을 위해 비밀번호는 평문으로 저장하지 않고 암호화합니다.

### 1-1. 사용법

```java
@Autowired
private BCryptPasswordEncoder bcrypt;

// 1. 암호화 (회원가입 시)
// 입력받은 비밀번호를 암호문으로 변환
String encPwd = bcrypt.encode("평문비밀번호");

// 2. 검증 (로그인 시)
// 결과는 boolean 반환 (일치: true, 불일치: false)
// matches(입력한평문, DB저장된암호문) 순서 주의
if(bcrypt.matches("inputPwd", "dbHashPwd")) {
    // 로그인 성공
}
```

## 2. 페이징 처리 (PageInfo & RowBounds)
#### PageInfo
```java
@AllArgsConstructor
@NoArgsConstructor
@Data
@Builder
public class PageInfo {
	public int currentPage;
	public int listCount;
	
	public int boardLimit;
	public int PageLimit;
	
	public int maxPage;
	public int startPage;
	public int endPage;
}
```
### 1. PageInation
```java
public class Pagination {
	
	//페이징 처리용 PageInfo 객체에 필드를 담아주는 메소드 (계산처리까지)
		public static PageInfo getPageInfo(int listCount,int currentPage
									  ,int boardLimit,int pageLimit) {
		
		int maxPage = (int)Math.ceil((double)listCount/boardLimit);
		int startPage = (currentPage-1)/pageLimit*pageLimit+1;
		int endPage = startPage+pageLimit-1;
		
		if(maxPage<endPage) {
			endPage = maxPage;
		}
		
		//PageInfo pi = new PageInfo(currentPage,listCount,boardLimit,pageLimit,maxPage,startPage,endPage);
		
		PageInfo pi =	PageInfo.builder()
								.listCount(listCount)
							    .currentPage(currentPage)
							    .boardLimit(boardLimit)
							    .pageLimit(pageLimit)
							    .maxPage(maxPage)
							    .startPage(startPage)
							    .endPage(endPage)
							    .build(); 
		//lombok의 @Builder 어노테이션을 이용하여 생성할 각 필드를 메소드처럼 사용하여 값을 채워줄 수 있다.
		//필요한 필드만 채워넣을 수 있는 장점이 있음,값을 넣는 순서 중요하지 않음
							    
		return pi;
	}
```
### Controller
```java
	@RequestMapping("/list.bo")
	public String boardList(@RequestParam(value="page",defaultValue = "1")
						 	int currentPage
						   ,Model model) {
		//게시글 목록 
		//페이징처리에 필요한 요소들 
		//현재페이지값 : currentPage
		//게시글 총 개수 조회해오기 
		int listCount = service.listCount();
		int boardLimit = 5; //게시글 보여줄 개수
		int pageLimit = 10; //페이징바 개수
		
		PageInfo pi = Pagination.getPageInfo(listCount, currentPage, boardLimit, pageLimit);
		
		ArrayList<Board> list = service.boardList(pi);
		
		model.addAttribute("pi",pi);
		model.addAttribute("list",list);
		
		return "board/boardListView";
	}
```

### 3. Dao
- RowBounds 계산 및 조회
```java
// offset: 건너뛸 게시글 수 (현재페이지 - 1) * 한페이지한도
int limit = pageinfo.getBoardLimit();
int offset = (pageinfo.getCurrentPage()-1) * limit;

RowBounds rowBounds = new RowBounds(offset, limit);

// 세 번째 인자로 rowBounds 전달
List<Board> list = sqlSession.selectList("boardMapper.boardList", null, rowBounds);
```

#### contextRoot 가져오는 방법들
| **코드**                                    | **환경**                     | **설명**                                                                                   |
| ----------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------- |
| **`request.getContextPath()`**            | **Java/JSP (서버 사이드)**      | 서버에서 실행되는 Java 코드입니다. JSP 파일 내 `<% ... %>` 스크립트릿 안에서 사용됩니다.                              |
| **`$ {pageContext.request.contextPath}`** | **JSP/EL (서버 사이드)**        | EL 문법을 사용하여 Java 객체의 속성을 가져와 **텍스트로 출력**합니다. `<script>` 태그 안에서도 이 방식으로 서버 데이터를 가져와야 합니다. |
| **`location.href = ...`**                 | **JavaScript (클라이언트 사이드)** | 브라우저(클라이언트)에서 실행되는 코드입니다. JSP가 출력한 **최종 문자열**을 사용해야 합니다.                                 |
- JSP Tag`<a href="<c:url value='/' /> "></a>`
- Spring Redirect: `/` (자동 포함)


-----
