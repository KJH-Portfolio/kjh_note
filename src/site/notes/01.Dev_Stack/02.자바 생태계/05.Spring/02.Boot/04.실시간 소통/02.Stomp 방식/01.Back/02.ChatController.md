---
{"dg-publish":true,"permalink":"/01-dev-stack/02/05-spring/02-boot/04/02-stomp/01-back/02-chat-controller/"}
---

```java
package com.kh.spring.chat.controller;

import com.kh.spring.chat.model.dto.ChatMessageDto;
import com.kh.spring.chat.model.service.ChatService;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.messaging.handler.annotation.MessageMapping;
import org.springframework.messaging.simp.SimpMessagingTemplate;
import org.springframework.stereotype.Controller;

@Controller
@RequiredArgsConstructor
@Slf4j
public class ChatController {

    private final SimpMessagingTemplate messagingTemplate;
    private final ChatService chatService;

    // 클라이언트 발송 경로: /app/chat/message
    @MessageMapping("/chat/message")
    public void sendMessage(ChatMessageDto messageDto) {
        log.info("메시지 수신: {}", messageDto);
        
        try {
            // 1. DB에 메시지 먼저 안전하게 저장 (누가, 어떤 방에 남겼는지 테이블 영속화)
            ChatMessageDto savedMessage = chatService.saveMessage(messageDto);
            log.info("✅ 저장된 메시지 - messageId: {}", savedMessage.getMessageId());
            
            // 2. 메시지가 성공적으로 저장되었다면 해당 방을 구독하는 전체 클라이언트에게 발송
            messagingTemplate.convertAndSend("/topic/chat/room/" + messageDto.getChatRoomId(), savedMessage);
            
            // 3. 백그라운드 이벤트 트리거 (안 읽음 뱃지 증감, 타 접속 기기 푸시 알람 등 처리)
            chatService.sendGlobalNotifications(savedMessage);
            
        } catch (IllegalArgumentException e) {
            log.error("메시지 전송 실패 (유효성 검증): {}", e.getMessage());
            
            // [에러 처리 1] 방이 존재하지 않거나 권한이 없을 경우, 보낸 사람 개인 채널로만 조용히 1:1 에러 사유를 전송
            ChatMessageDto errorMsg = ChatMessageDto.builder()
                    .messageType("ERROR") 
                    .content(e.getMessage())
                    .chatRoomId(messageDto.getChatRoomId())
                    .build();
            messagingTemplate.convertAndSend("/topic/user/" + messageDto.getSenderId(), errorMsg);
            
        } catch (Exception e) {
            // [에러 처리 2] DB 연결 단절 등 알 수 없는 서버 에러 예외 대응
             ChatMessageDto errorMsg = ChatMessageDto.builder()
                    .messageType("ERROR") 
                    .content("메시지 전송 중 오류가 발생했습니다.")
                    .chatRoomId(messageDto.getChatRoomId())
                    .build();
            messagingTemplate.convertAndSend("/topic/user/" + messageDto.getSenderId(), errorMsg);
        }
    }
}
```